version: "3"
dotenv:
  - .env
tasks:
  fmt: mix format
  dev:
    cmds:
      - docker-compose up -d
      - sleep 2
      - mix ecto.migrate
      - iex --sname hypr --cookie hypr -S mix phx.server
      - docker-compose down
