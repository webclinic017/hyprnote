[package]
name = "llm"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
tokio = { workspace = true }

candle-core = { version = "0.8.1", features = ["metal"] }
candle-examples = { git = "https://github.com/huggingface/candle", tag = "0.8.1", features = ["metal"] }
candle-transformers = { version = "0.8.1", features = ["metal"] }
tokenizers = "0.19.1"
