[package]
name = "vad"
version = "0.1.0"
edition = "2021"

[dependencies]
hypr-onnx = { path = "../onnx", package = "onnx" }

dasp = { workspace = true }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
