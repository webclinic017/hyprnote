use serde::de::DeserializeOwned;
use tauri::{plugin::PluginApi, AppHandle, Runtime};

use crate::models::*;

pub fn init<R: Runtime, C: DeserializeOwned>(
    app: &AppHandle<R>,
    _api: PluginApi<R, C>,
) -> crate::Result<CoreAudio<R>> {
    Ok(CoreAudio(app.clone()))
}

/// Access to the core-audio APIs.
pub struct CoreAudio<R: Runtime>(AppHandle<R>);

impl<R: Runtime> CoreAudio<R> {
    pub fn ping(&self, payload: PingRequest) -> crate::Result<PingResponse> {
        Ok(PingResponse {
            value: payload.value,
        })
    }
}
