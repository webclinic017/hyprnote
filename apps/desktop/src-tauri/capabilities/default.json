{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "positioner:default",
    "fs:default",
    "sql:default",
    "sql:allow-execute",
    "store:default",
    "stronghold:default",
    "shell:allow-open"
  ]
}
