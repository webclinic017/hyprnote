[default]
description = "Default permissions for the plugin"
permissions = [
    "allow-get-status",
    "allow-load-model",
    "allow-unload-model",
    "allow-start-server",
    "allow-stop-server",
]
